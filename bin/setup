#!/usr/bin/env bash

bundle install

# Do any other automated setup that you need to do here

if command -v openscad >/dev/null 2>&1; then
  echo -e "OpenSCAD already installed"; else
  sudo apt install openscad -y >/dev/null 2>&1;
  echo -e "OpenSCAD installed!";
fi
